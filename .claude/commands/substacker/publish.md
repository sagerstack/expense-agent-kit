Invoke the `substacker-publish` skill to publish a completed article to Substack using Chrome browser automation.

If a slug argument is provided, publish that specific article. Otherwise, find the most recent article with status `drafted` in `docs/articles/index.md`.

$ARGUMENTS
