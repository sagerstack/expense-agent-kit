Invoke the `sagerstack-code-planning` skill.

This is the mandatory first step before any implementation. It produces `docs/project-context.md` with the Objective, Milestones with Key Results, and Epic breakdown through an interactive Q&A session.

User input: $ARGUMENTS
