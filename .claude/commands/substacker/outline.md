Invoke the `substacker-outline` skill to deeply research a topic and produce a structured article outline.

If a slug argument is provided, outline that specific article. Otherwise, find the most recent article with status `discovered` in `docs/articles/index.md`.

$ARGUMENTS
