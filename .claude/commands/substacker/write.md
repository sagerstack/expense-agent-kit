Invoke the `substacker-write` skill to write a full article from an approved outline.

If a slug argument is provided, write that specific article. Otherwise, find the most recent article with status `outlined` in `docs/articles/index.md`.

$ARGUMENTS
