Invoke the `substacker-discover` skill to find trending tech/AI/ML topics for a Substack article.

If the user provided a topic after the command, use that as the on-demand topic input instead of running the full discovery scan.

$ARGUMENTS
