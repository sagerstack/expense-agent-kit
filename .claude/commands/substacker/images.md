Invoke the `substacker-images` skill to generate all visual assets for an article — hero images, diagrams, and code screenshots.

If a slug argument is provided, generate images for that specific article. Otherwise, find the most recent article with status `drafted` in `docs/articles/index.md`.

$ARGUMENTS
